<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Tailwind output -->
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>

<div class="min-h-screen d-flex justify-content-center align-items-center p-5 
            position-relative overflow-hidden
            bg-gradient-to-br from-[#C9B3FF] to-[#B8A3FF] 
            bg-[length:200%_200%] animate-gradientShift">

  <!-- Floating lights -->
  <div class="position-absolute w-50 h-50 bg-white/10 rounded-circle 
              top-0 end-0 translate-middle animate-float"></div>

  <div class="position-absolute w-50 h-50 bg-white/10 rounded-circle 
              bottom-0 start-0 translate-middle animate-floatReverse"></div>

  <!-- Card -->
  <div class="bg-white rounded-4 shadow-lg w-100 p-4" style="max-width: 420px;">

    <!-- Logo -->
    <div class="text-center mb-4">
      <div class="mx-auto mb-3 rounded-circle d-flex justify-content-center 
                  align-items-center overflow-hidden"
           style="width: 80px; height: 80px; ">
        <img src="/assets/logomoody.png" width="82" class="rounded-circle">
      </div>

      <!-- MERGED VERSION -->
      <h1 class="fw-bold">Moody</h1>
      <p class="text-muted">Welcome! We missed you.</p>
    </div>

    <!-- Login Form -->
    <form action="/login" method="POST">

      <div class="mb-3">
        <label class="form-label">Email Address</label>
        <input type="email" name="email" class="form-control" placeholder="example@gmail.com">
      </div>

      <div class="mb-3">
        <label class="form-label">Password</label>
        <input type="password" name="password" class="form-control" placeholder="••••••••">
      </div>

      <div class="text-end mb-3">
        <a href="/forgot" class="text-decoration-none text-muted">Forgot password?</a>
      </div>

      <button class="btn w-100 text-white fw-semibold py-2"
              style="background: linear-gradient(to right, #9B7EFF, #B8A3FF); border: none;">
        Log In
      </button>
    </form>

    <!-- Divider -->
    <div class="text-center my-4 position-relative">
      <span class="bg-white px-2 text-muted">or</span>
      <hr>
    </div>

    <p class="text-center text-muted">
      Don't have an account?
      <a href="/signup" class="fw-semibold text-primary text-decoration-none">Sign up</a>
    </p>

  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
